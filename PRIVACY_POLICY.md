# Privacy Policy

Tab Audio Normalizer processes audio locally in the browser.

## Data Collection

- No audio is uploaded or transmitted.
- No browsing history is collected.
- No personal data is sold or shared.

## Permissions

- `tabs` is used to identify the active tab for the popup UI.
- `storage` is used to save per-site enable/disable state locally.
- `host_permissions` are used so the extension can normalize audio on sites the user enables.

## Contact

If this policy needs a support contact or website URL, add it before Chrome Web Store submission.
